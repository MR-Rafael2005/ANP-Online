Tarefas:
-Cadastro [X]
-Login [X]
-Perfil []
-Edição Usuario []
-Pagina principal [X]
-Anuncios []
-Notificações(Por email tbm) []
-Turmas []
-Alunos []
-RAC []
-Ocorrencias []

-Cores:
#46e44c
#3eca43
#37b13b
#2f9832
#277f2a
#1f6522
#174c19
#103311
#081908
#000000

Cargos:
-Administrador
-Usuario
-Ingressante

PARA RESOLVER:
- O firebase não disponibiliza mais storage, fotos serão salvas em base64 [Resolvido]

- Para expandir o projeto devo criar um arquivo js que importe um html com navbar/rodapé/barra laretal e etc, tornará o projeto mais "expandivel", como fazer:
fetch("arquivo_com.html").then(res => res.text()).then(data => document.getElementById('navbar-include').innerHTML = data)

- Usar Apache Cordova/PhoneGap para fazer o apk

EMAIL FINAL: 23247754@prof.ce.gov.br (Atualmente esta no meu email pessoal)


FIREBASE CLI:

# Deploy completo (hosting + firestore rules + functions)
firebase deploy

# Deploy apenas hosting
firebase deploy --only hosting

# Deploy apenas regras do Firestore
firebase deploy --only firestore:rules

# Deploy apenas índices do Firestore
firebase deploy --only firestore:indexes